box: debian:7.9
build:
  steps:
    - script:
        name: update
        code: |
            apt-get update
    - script:
        name: module install
        code: |
            apt-get -y install apache2 php5
